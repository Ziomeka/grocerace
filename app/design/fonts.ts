import type { WebFontsOptions } from '@unocss/preset-web-fonts'
export const fonts: WebFontsOptions = {
  provider: 'google',
  fonts: {
    sans: 'Inter',
    heading: 'Fredoka',
  }
}

export const fontFamily = {
  sans: 'Inter, sans-serif',
  heading: 'Fredoka, Helvetica, sans-serif',
}